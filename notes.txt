next: 
[x] separate out external libraries and test build 
[x] separate server and client to different containers 
[x] test networking across containers in different machines
[x] Add download, build and install of opencv to base.Dockerfile
[x] Add necessary commands to include opencv in Makefile
- Test by importing Opencv to current UDP code
- send images and display on serverside
- 


g++ `pkg-config –cflags opencv` file_name.cpp -o output_file `pkg-config –libs opencv`
g++ `pkg-config –cflags opencv` file_name.cpp -o output_file `pkg-config –libs opencv`
g++ `pkg-config -–cflags opencv` file_name.cpp -o output_file `pkg-config –-libs opencv`

root@linuxkit-025000000001:/server# apt-file search opencv.pc
libopencv-dev: /usr/lib/x86_64-linux-gnu/pkgconfig/opencv.pc

export PKG_CONFIG_PATH=$PKG_CONFIG_PATH:/usr/lib/x86_64-linux-gnu/pkgconfig